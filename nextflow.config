
process.executor = 'local'

docker.enabled = true
docker.runOptions = '-u \$(id -u):\$(id -g)'

params {
    input1h = "data/dds_forDeseq_transcrSummarized.RData"
    input4w = "data/4_weeks/dds_forDeseq.RData"
    output  = "results/"
}

singularity.enabled = false

